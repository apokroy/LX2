/* xsltlocale.c includes <winnls.h> after <windows.h>; everything it needs is already
 * declared in the windows.h of this directory. */
#ifndef LX2_SHIM_WINNLS_H
#define LX2_SHIM_WINNLS_H
#include "windows.h"
#endif
